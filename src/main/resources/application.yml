spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/personal?useSSL=false&characterEncoding=UTF-8
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
      show-sql : true
      properties:
        hibernate:
          dialect : org.hibernate.dialect.MySQL5Dialect
      hibernate:
        ddl-auto: none
server:
  port: 8888